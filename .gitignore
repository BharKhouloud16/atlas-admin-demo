node_modules/
.next/
.env
*.log

# Playwright (voir playwright.config.ts, .github/workflows/ci.yml)
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# Sessions pré-authentifiées produites par tests/setup/auth.setup.ts —
# jamais commitées (état de session, pas du code, régénérées à chaque run).
/.auth/
